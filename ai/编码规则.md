**对项目进行分析**

对整个Java项目进行全面的代码结构分析，系统梳理所有类、接口及其实现类之间的关系。具体要求包括：识别并记录各个类之间的继承关系（如类A继承自类B）、接口实现关系（如类C实现接口D）、关联关系（如类E包含类F的实例作为成员变量）、依赖关系（如方法参数或局部变量中使用其他类）等。需绘制清晰的类图，使用标准UML符号表示各类关系，并为每个关系提供代码层面的具体证据（如extends关键字、implements关键字、成员变量声明等）。分析结果应包含项目中所有核心类和接口的关系说明，明确各组件在系统架构中的位置和作用。



**软件规则**

1. 请保持对话语言为中文
2. 如果需要使用python的话，请使用pip3 和 python3 命令。
3. 请在生成代码时添加函数级注释，所有生成的函数和代码都必须添加注释进行说明
4. 之前完成正确的功能,尽量不要修改。比如当前的instruction是完善功能的,那么只需要专注功能A,不需要修改其他功能（比如功能B）。但是如果A文件的修改涉及到了B文件那么对应的B文件也要进行修改。
5. 生成的注释用中文,并使用 UTF-8 编码。
6. 生成的代码有时候会存在中文乱码的情况,所以你在生成中文的时候,需要检查是否有中文乱码,如果有乱码需要修正。
7. 如果修改某个函数的实现,先理解之前函数实现的逻辑。然后在原来的基础上,再进行修改（保留之前的函数逻辑,不要移除）
8. 进行代码编辑修改完成之后，要进行检查代码的修改是否造成了其他的错误的发生。
9. 对文件的修改编辑之前，要先给出给出你的操作方案，展示给我进行确认。

**JAVA项目规则**

1. 在java代码中对类或者接口函数方法的修改要同时检查是否涉及到了继承类、实现类、关联类并进行响应的修改调整。